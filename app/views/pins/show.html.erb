<p id="notice"><%= notice %></p>

<p>
  <strong>Nombre:</strong>
  <%= @pin.name %>
</p>
<p>
  <strong>Photo:</strong>
  <%= image_tag(@pin.photo, :style =>'width:200px') %>
</p>

<p>
  <strong>Description:</strong>
  <%= @pin.description %>
</p>

<%= link_to 'Edit', edit_pin_path(@pin) %> |
<%= link_to 'Back', pins_path %>

<%= link_to "Más pins de este usuario", pinsof_path(@pin.user.id) %>
